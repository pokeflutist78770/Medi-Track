

Requires:
  -Java 1.8
  
  
  
Run  MainMenu.java to start the application

This was an eclipse project, so it is a little weird now. But as long as you put all the Java files in the same directory and compile you should be good. 